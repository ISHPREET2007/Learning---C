#include <stdio.h>
int main()
{
    int a, b;
    a = 12, b = 13;
    /* then there are various type of arithmetic operators.
     like for power ^ not used  (fact)
    then there is type conversion like int and int give int ,
    float and int give float,
    demolition concept
     now will do predecence priority basis operator*/
    return 0;
}